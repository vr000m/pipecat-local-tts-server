"""Async TTS client (stub).

Phase 0 scaffolding. Phase 1 implements ``TTSClient`` — ``connect() -> hello``,
``update()``, ``append()``, ``commit()``, ``cancel()``, ``events()``,
``status()``, ``close()``. Transport-generic (no app labels or frame types; the
pipecat-framework adapter lives in ``examples/``).

stdlib + websockets only.
"""

from __future__ import annotations
